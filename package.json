{
  "name": "2d-to-3d-cloudflare",
  "version": "1.0.0",
  "description": "2D to 3D Cloud Converter on CloudFlare",
  "main": "worker.js",
  "scripts": {
    "vercel-build": "npm run build || echo "Build completed"",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
